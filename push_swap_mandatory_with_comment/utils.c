#include "push_swap.h"

// Временная функция, потом удалить
void ft_print(t_list *lst)
{
    // printf ("[ ft_print: ");
    // while (lst)
    // {
    //     printf (" %d ", lst->content);
    //     lst = lst->next;
    // }
    // printf ("]\n");
    if(lst)
    {}
}  

void ft_print_rank(t_list *lst)
{
    // printf ("[ft__rank: ");
    while (lst)
    {
        // printf (" %d ", lst->rank);
        lst = lst->next;
    }
    // printf ("]\n");
}